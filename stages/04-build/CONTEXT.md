# Stage 04 — Build

**State:** ⏳ not started
**Gate:** web build and iOS build both implemented off the 02 design system and
the 03 platform spec — no undecided design or platform questions left open.

## Scope (when picked up)
- Web: apply 02's tokens/assets, remove Grok sandbox scaffolding per 03's plan.
- iOS: implement the native wrapper/shell decided in 03.
- Keep both builds on one source of truth (calculators in `src/lib/calc/`) —
  no forked logic between platforms.

## Do not start without Andre's go-ahead — this workspace is held at the design
stage per the current instruction. This stage is scaffolded only; nothing here
should be built until 02 and 03 are both signed off.
