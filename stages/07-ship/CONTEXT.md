# Stage 07 — Ship

**State:** ⏳ not started
**Gate:** web deployed to its assigned domain (setout.online/.site per 03's role
split) and the iOS build submitted (TestFlight → App Store review).

Per [[feedback_confirm_before_each_deploy]]: get an explicit go-ahead for each
deploy, not a standing approval from an earlier one.

## Do not start without Andre's go-ahead — this workspace is held at the design
stage per the current instruction.
